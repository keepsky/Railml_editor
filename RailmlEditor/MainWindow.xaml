<Window x:Class="RailmlEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RailmlEditor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Open..."/>
                <MenuItem Header="Save..."/>
                <Separator/>
                <MenuItem Header="Exit"/>
            </MenuItem>
            <MenuItem Header="Edit" />
            <MenuItem Header="View" />
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="Ready"/>
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Toolbox -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="0,0,1,0" Background="#F0F0F0">
                <StackPanel Margin="10">
                    <TextBlock Text="Toolbox" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Button Content="Track" Margin="0,5"/>
                    <Button Content="Switch" Margin="0,5"/>
                    <Button Content="Signal" Margin="0,5"/>
                </StackPanel>
            </Border>

            <!-- Main Canvas -->
            <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Canvas x:Name="MainCanvas" Background="White" Width="2000" Height="2000">
                    <!-- Grid Lines or background can go here -->
                </Canvas>
            </ScrollViewer>

            <!-- Properties Panel -->
            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1,0,0,0" Background="#F0F0F0">
                <StackPanel Margin="10">
                    <TextBlock Text="Properties" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Select an item to view properties." Foreground="Gray"/>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
