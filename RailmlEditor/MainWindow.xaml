<Window x:Class="RailmlEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RailmlEditor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- Data Templates for Visual Elements -->
        <DataTemplate DataType="{x:Type local:ViewModels.TrackViewModel}">
            <Rectangle Width="{Binding Length}" Height="10" Fill="Black" Stroke="Gray" StrokeThickness="1"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:ViewModels.SwitchViewModel}">
            <Polygon Points="0,0 40,0 20,20 0,0" Fill="Orange" Stroke="Black" StrokeThickness="1"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:ViewModels.SignalViewModel}">
            <Ellipse Width="15" Height="15" Fill="Red" Stroke="DarkRed" StrokeThickness="1"/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Open..." Click="FileOpen_Click"/>
                <MenuItem Header="Save..." Click="FileSave_Click"/>
                <Separator/>
                <MenuItem Header="Exit" Click="FileExit_Click"/>
            </MenuItem>
            <MenuItem Header="Edit" />
            <MenuItem Header="View" />
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="Ready"/>
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Toolbox -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="0,0,1,0" Background="#F0F0F0">
                <StackPanel Margin="10">
                    <TextBlock Text="Toolbox" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Button Content="Track" Margin="0,5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                    <Button Content="Switch" Margin="0,5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                    <Button Content="Signal" Margin="0,5" PreviewMouseLeftButtonDown="Toolbox_PreviewMouseLeftButtonDown"/>
                </StackPanel>
            </Border>

            <!-- Main Canvas Area -->
            <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="MainDesigner" 
                              ItemsSource="{Binding Elements}" 
                              Background="White" Width="2000" Height="2000"
                              AllowDrop="True"
                              Drop="MainDesigner_Drop">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding X}"/>
                            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                            <EventSetter Event="PreviewMouseDown" Handler="Item_MouseDown"/>
                            <EventSetter Event="PreviewMouseMove" Handler="Item_MouseMove"/>
                            <EventSetter Event="PreviewMouseUp" Handler="Item_MouseUp"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentPresenter Content="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Properties Panel -->
            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1,0,0,0" Background="#F0F0F0">
                <StackPanel Margin="10">
                    <TextBlock Text="Properties" FontWeight="Bold" Margin="0,0,0,10"/>
                    <!-- Simple Property Grid for now -->
                    <Grid DataContext="{Binding SelectedElement}">
                        <StackPanel>
                            <TextBlock Text="ID:"/>
                            <TextBox Text="{Binding Id}"/>
                            <TextBlock Text="X:"/>
                            <TextBox Text="{Binding X}"/>
                            <TextBlock Text="Y:"/>
                            <TextBox Text="{Binding Y}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
