<UserControl x:Class="RailmlEditor.Views.ExplorerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RailmlEditor"
    xmlns:viewmodels="clr-namespace:RailmlEditor.ViewModels" mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.Resources>
        <!-- Custom TreeViewItem Style for connecting lines -->
        <Style x:Key="RailmlTreeViewItemStyle" TargetType="{x:Type TreeViewItem}">
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=OneWay}"/>
            <Setter Property="Padding" Value="1,0,0,0"/>
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="TreeViewItem_PreviewMouseLeftButtonDown"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="19"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Line x:Name="VerticalLine" Grid.Column="0" Grid.RowSpan="2" X1="9.5" Y1="0" X2="9.5" Y2="1" Stretch="Fill" Stroke="#A0A0A0" StrokeThickness="1" StrokeDashArray="1,2" SnapsToDevicePixels="True"/>
                            <Line x:Name="HorizontalLine" Grid.Column="0" X1="9.5" Y1="12" X2="19" Y2="12" Stroke="#A0A0A0" StrokeThickness="1" StrokeDashArray="1,2" SnapsToDevicePixels="True"/>

                            <ToggleButton x:Name="Expander" Style="{StaticResource ExpandCollapseToggleStyle}" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press" VerticalAlignment="Center"/>

                            <Border x:Name="Bd" Grid.Column="1" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true" VerticalAlignment="Center" MinHeight="22">
                                <ContentPresenter x:Name="PART_Header" ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>

                            <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Grid.Column="1" Margin="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <TextBlock Text="Explorer" DockPanel.Dock="Top" Background="#F0F0F0" Padding="5" FontWeight="Bold"/>
        <TreeView x:Name="ElementTree" ItemsSource="{Binding TreeRoots}" BorderThickness="0" RequestBringIntoView="TreeView_RequestBringIntoView">
            <TreeView.Resources>
                <!-- Level 1: File -->
                <!-- Level 1: Infrastructure -->
                <HierarchicalDataTemplate DataType="{x:Type viewmodels:InfrastructureViewModel}" ItemsSource="{Binding Categories}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/images/london.png" Width="16" Height="16" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                    </StackPanel>
                </HierarchicalDataTemplate>

                <!-- Level 2: Category -->
                <HierarchicalDataTemplate DataType="{x:Type viewmodels:CategoryViewModel}" ItemsSource="{Binding Items}">
                    <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                </HierarchicalDataTemplate>

                <!-- Level 3: Elements -->
                <HierarchicalDataTemplate DataType="{x:Type viewmodels:TrackViewModel}" ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id}"/>
                        <TextBlock Text=" - " Foreground="Gray"/>
                        <TextBlock Text="{Binding Name}" Foreground="Gray" FontStyle="Italic"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
                <HierarchicalDataTemplate DataType="{x:Type viewmodels:CurvedTrackViewModel}" ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id}"/>
                        <TextBlock Text=" - " Foreground="Gray"/>
                        <TextBlock Text="{Binding Name}" Foreground="Gray" FontStyle="Italic"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
                <DataTemplate DataType="{x:Type viewmodels:TrackNodeViewModel}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Role}" FontWeight="Bold" Foreground="DarkMagenta"/>
                        <TextBlock Text="(" Foreground="Gray"/>
                        <TextBlock Text="{Binding TypeName}" Foreground="Purple"/>
                        <TextBlock Text=") - " Foreground="Gray"/>
                        <TextBlock Text="{Binding Id}"/>
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewmodels:TrackCircuitBorderViewModel}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id}" Foreground="Navy"/>
                        <TextBlock Text="-" Foreground="Gray"/>
                        <TextBlock Text="{Binding Name}" Foreground="Navy" FontStyle="Italic"/>
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewmodels:SignalViewModel}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id}"/>
                        <TextBlock Text="(" Foreground="Gray"/>
                        <TextBlock Text="{Binding Type}" Foreground="Gray"/>
                        <TextBlock Text=") - " Foreground="Gray"/>
                        <TextBlock Text="{Binding Name}" Foreground="Gray" FontStyle="Italic"/>
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewmodels:SwitchViewModel}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id}"/>
                        <TextBlock Text=" - " Foreground="Gray"/>
                        <TextBlock Text="{Binding Name}" Foreground="Gray" FontStyle="Italic"/>
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewmodels:RouteViewModel}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id}"/>
                        <TextBlock Text=" - " Foreground="Gray"/>
                        <TextBlock Text="{Binding Name}" Foreground="Gray" FontStyle="Italic"/>
                    </StackPanel>
                </DataTemplate>
                <HierarchicalDataTemplate DataType="{x:Type viewmodels:AreaViewModel}" ItemsSource="{Binding Borders}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id}" FontWeight="Bold" Foreground="DarkGreen"/>
                        <TextBlock Text=" - " Foreground="Gray"/>
                        <TextBlock Text="{Binding Name}" Foreground="DarkGreen" FontStyle="Italic"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource RailmlTreeViewItemStyle}"/>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </DockPanel>
</UserControl>
