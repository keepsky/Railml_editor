<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Material Design Color Palette Adapter (Light Theme) -->

    <!-- Backgrounds -->
    <SolidColorBrush x:Key="MaterialDesignPaper" Color="#F5F5F5"/>
    <!-- Off-white background -->
    <SolidColorBrush x:Key="MaterialDesignCardBackground" Color="#FFFFFF"/>
    <!-- Pure white cards -->
    <SolidColorBrush x:Key="MaterialDesignToolBarBackground" Color="#E0E0E0"/>

    <!-- Text / Foreground -->
    <SolidColorBrush x:Key="MaterialDesignBody" Color="#DD000000"/>
    <!-- Black text -->
    <SolidColorBrush x:Key="MaterialDesignBodyLight" Color="#89000000"/>

    <!-- Dividers & Borders -->
    <SolidColorBrush x:Key="MaterialDesignDivider" Color="#1F000000"/>
    <SolidColorBrush x:Key="MaterialDesignSelection" Color="#30673AB7"/>
    <!-- Purple tint -->

    <!-- Accents (Keep same branding or adjust slightly) -->
    <SolidColorBrush x:Key="PrimaryHueMidBrush" Color="#673AB7"/>
    <!-- DeepPurple -->
    <SolidColorBrush x:Key="PrimaryHueLightBrush" Color="#9575CD"/>
    <SolidColorBrush x:Key="PrimaryHueDarkBrush" Color="#512DA8"/>

    <SolidColorBrush x:Key="SecondaryHueMidBrush" Color="#CDDC39"/>
    <!-- Lime -->
    <SolidColorBrush x:Key="SecondaryHueLightBrush" Color="#F0F4C3"/>
    <SolidColorBrush x:Key="SecondaryHueDarkBrush" Color="#AFB42B"/>

    <!-- Standard Styles -->
    <Style x:Key="MaterialDesignToolBar" TargetType="{x:Type ToolBar}">
        <Setter Property="Background" Value="{StaticResource MaterialDesignToolBarBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource MaterialDesignBody}"/>
    </Style>

    <!-- Grid Background Brush (Light Mode) -->
    <DrawingBrush x:Key="GridBackgroundBrush" Viewbox="0,0,10,10" ViewboxUnits="Absolute" Viewport="0,0,10,10" ViewportUnits="Absolute" TileMode="Tile">
        <DrawingBrush.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="Transparent">
                    <GeometryDrawing.Geometry>
                        <RectangleGeometry Rect="0,0,10,10"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
                <GeometryDrawing Brush="#D3D3D3">
                    <GeometryDrawing.Geometry>
                        <RectangleGeometry Rect="0,0,1,1"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingBrush.Drawing>
    </DrawingBrush>

</ResourceDictionary>
